---

- name: test if docker service is running
  command: systemctl status docker
  ignore_errors: true
  changed_when: false
  register: docker_status
  no_log: "{{ debug_hide }}"

- name: stop playbok as docker is not running
  meta: end_play
  when: not(docker_status)
